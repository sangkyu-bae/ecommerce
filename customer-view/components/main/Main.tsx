import React from 'react';
import {StyledContainer} from "@/api/common/GridComponent";

function Main(props) {
    return (
        <StyledContainer>

        </StyledContainer>
    );
}

export default Main;